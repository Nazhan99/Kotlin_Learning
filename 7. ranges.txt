fun main(args: Array<String>) {
    //ranges

    val r1=1..5 //range contain number 1,2,3,4,5
    val r2=50 downTo 1 // range from 50 to 1
    val r3=5 downTo 1 step 2 //range number 5,3,1
    var r4='a'..'z' // range a to z

    var isPresent ='c' in r4

    var countDown= 10.downTo(1) // number 10 to 1
    var moveUp= 1.rangeTo(10) //range from 1 to 10
}

